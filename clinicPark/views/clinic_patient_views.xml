<odoo>
    <!-- Acción -->
    <record id="clinic_patient_action" model="ir.actions.act_window">
        <field name="name">Patient Admissions</field>
        <field name="res_model">clinic.patient</field>
        <field name="view_mode">tree,form</field>
    </record>

    <!-- Vista Tree -->
    <record id="clinic_patient_tree_view" model="ir.ui.view">
        <field name="name">clinic.patient.tree</field>
        <field name="model">clinic.patient</field>
        <field name="arch" type="xml">
            <tree string="Patients">
                <field name="name"/>
                <field name="age"/>
                <field name="gender"/>
                <field name="admission_date"/>
            </tree>
        </field>
    </record>

    <!-- Vista Form centrada -->
    <record id="clinic_patient_form_view" model="ir.ui.view">
        <field name="name">clinic.patient.form</field>
        <field name="model">clinic.patient</field>
        <field name="arch" type="xml">
            <form string="Patient Admission">
                <sheet>
                    <group>
                        <group string="Patient Information" col="2">
                            <field name="name"/>
                            <field name="age"/>
                            <field name="gender"/>
                            <field name="phone"/>
                            <field name="email"/>
                            <field name="address"/>
                        </group>
                        <group string="Companion Information" col="2">
                            <field name="companion_name"/>
                            <field name="companion_phone"/>
                            <field name="companion_relationship"/>
                        </group>
                        <group string="Admission Details" col="2">
                            <field name="admission_reason"/>
                            <field name="admission_date"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
</odoo>
